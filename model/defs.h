#ifndef CHESS_DEFS_H
#define CHESS_DEFS_H

#include <stdint.h>
#include <wchar.h>

// single byte boolean, because why is the built-in one 4 bytes...
typedef uint8_t bool_t;
#define true 1
#define false 0

#endif
